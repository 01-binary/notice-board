import React, { FC } from 'react';
import Button from '@src/components/Button';

import * as S from './style';

const Tool: FC = () => {
  return (
    <S.Container>
      <Button size={'medium'}>
        asd
      </Button>
      <Button size={'medium'}>
        asd
      </Button>
    </S.Container>
  );
};

export default Tool;
